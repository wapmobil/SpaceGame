# Фаза 2: Core expedition chain engine (Go)

## Цель
Создать полностью рабочую систему expedition event chains на бэкенде. LLM генерация событий, обработка выборов, retry-логика, сохранение в БД.

## Что работает после деплоя
Методы `StartExpeditionChain()`, `ResolveChoice()`, `CompleteChain()`, `FailChain()` работают. API ещё нет.

## Задачи

### 2.1 Создать expedition_chain.go

**Файл:** `server/internal/game/planet_survey/expedition_chain.go` (NEW)

#### Структуры

```go
package planet_survey

import (
    "time"
)

// ExpeditionChain represents a running/completed expedition event chain.
type ExpeditionChain struct {
    ID                 string
    PlanetID           string
    OwnerID            string
    Status             string            // "active" | "completed" | "failed"
    EventCount         int
    CurrentEventIndex  int
    Events             []ExpeditionEvent // история событий в цепочке
    DiscoveredLocation *Location
    Inventory          map[string]float64
    CreatedAt          time.Time
    UpdatedAt          time.Time
}

// ExpeditionEvent represents a single event generated by LLM.
type ExpeditionEvent struct {
    EventID         string
    Description     string
    ImmediateReward map[string]float64
    Choices         []ExpeditionChoice
    IsEnd           bool
    LocationReward  string
}

// ExpeditionChoice represents a player choice within an event.
type ExpeditionChoice struct {
    Label       string
    Description string
    Reward      map[string]float64
    NextEventID string
}
```

#### Константы

```go
const (
    MaxInventory    = 1000.0
    MaxLLMRetries   = 5
    MaxChainDepth   = 6
    MaxActiveChains = 1  // лимит активных цепочек на планету
)

var ValidInventoryResources = []string{"food", "iron", "composite", "mechanisms", "reagents"}
```

#### Функции

**`ValidateInventory(inventory map[string]float64) error`**
- Сумма всех ресурсов <= 1000
- Все значения >= 0
- Только ресурсы из `ValidInventoryResources`
- Все значения > 0 (нельзя отправить пустой инвентарь)
- Возвращает error с понятным сообщением

**`BuildPrompt(planet *Planet, inventory map[string]float64, events []ExpeditionEvent) string`**
- Собирает JSON контекст:
  - planet.name, planet.description, planet.level, planet.ResourceType
  - inventory (текущие ресурсы экспедиции)
  - events (история событий — event_id, description, choices, player_choice, rewards_received)
  - expedition_depth = len(events)
  - max_depth = MaxChainDepth
- Подставляет контекст в prompt template
- Возвращает полный prompt для LLM

**`ParseReward(raw map[string]interface{}) (map[string]float64, error)`**
- Принимает raw JSON от LLM: `{"resources": {"reagents": 50, "food": -10}}`
- Извлекает `resources` → `map[string]float64`
- Фильтрует: оставляет только ресурсы из `ValidInventoryResources`
- Неизвестные ресурсы игнорируются
- Возвращает flat map: `{"reagents": 50, "food": -10}`

**`ApplyRewardToInventory(inventory map[string]float64, reward map[string]float64)`**
- Для каждого ресурса: `inventory[res] += amount`
- Clamp: если `inventory[res] < 0` → `inventory[res] = 0`
- Если `GetInventorySize(inventory) > MaxInventory` → пропорционально уменьшить до 1000

**`GetInventorySize(inventory map[string]float64) float64`**
- Сумма всех значений в inventory

**`ParseEventResponse(raw string) (*ExpeditionEvent, error)`**
- Принимает raw string от LLM
- Если markdown-обёртка (```json ... ```) — убирает
- Парсит JSON в ExpeditionEvent
- Валидирует: event_id не пустой, choices 2-4 шт (или 0 если is_end), is_end boolean
- Для `immediate_reward` и `choice.reward` — вызывает `ParseReward()`
- Генерирует event_id на бэкенде (UUID), игнорирует event_id от LLM
- Возвращает ошибку если JSON невалиден

**`GenerateEventWithRetry(prompt string) (*ExpeditionEvent, error)`**
- Вызывает LLM через `openai.New().Chat(ctx, prompt)` с context timeout 300s
- Retry до 5 раз с экспоненциальной задержкой: 1s, 2s, 4s, 8s, 16s
- После каждого вызова — `ParseEventResponse()`
- Генерирует event_id через uuid.New()
- Если парсинг не удался после 5 ретраев — возвращает ошибку

**`StartExpeditionChain(planet *Planet, inventory map[string]float64) (*ExpeditionChain, *ExpeditionEvent, error)`**
- Валидирует inventory (`ValidateInventory`)
- Создаёт `ExpeditionChain` с UUID (uuid.New()), переданным inventory
- Генерирует промпт (`BuildPrompt` с пустыми events)
- Генерирует первое событие с retry (`GenerateEventWithRetry`)
- Применяет `immediate_reward` к inventory через `ApplyRewardToInventory`
- Сохраняет событие в chain.Events
- Возвращает chain + первое событие

**`ResolveChoice(chain *ExpeditionChain, choiceIndex int) (*ExpeditionEvent, error)`**
- Валидирует choiceIndex (0 <= index < len(current_event.Choices))
- Применяет награду за выбор к `chain.Inventory` через `ApplyRewardToInventory`
- Записывает выбор и награды в историю
- Проверяет `is_end` текущего события:
  - Если `is_end == true`:
    - Если `location_reward != ""`: создаёт Location (SelectLocationType, GenerateName, CalculateSourceAmount)
    - Вызывает `CompleteChain(chain, location)`
    - Возвращает nil event (цепочка завершена)
  - Если `is_end == false`:
    - Генерирует следующее событие с retry
    - Применяет `immediate_reward` к inventory
    - Сохраняет в chain.Events, инкрементирует CurrentEventIndex
    - Возвращает новое событие
- При ошибке LLM — retry 5 раз, затем `FailChain(chain)` + возвращает ошибку

**`CompleteChain(chain *ExpeditionChain, location *Location)`**
- Устанавливает `chain.Status = "completed"`
- Устанавливает `chain.DiscoveredLocation = location`
- Обновляет `chain.UpdatedAt`

**`FailChain(chain *ExpeditionChain)`**
- Устанавливает `chain.Status = "failed"`
- Обновляет `chain.UpdatedAt`
- Инвентарь НЕ возвращается автоматически — это делает caller

**`ReturnInventoryToPlanet(planet *Planet, inventory map[string]float64)`**
- Возвращает ресурсы из inventory на планету
- Только ресурсы из `ValidInventoryResources`

**`GetCurrentEvent(chain *ExpeditionChain) *ExpeditionEvent`**
- Возвращает событие по индексу `chain.CurrentEventIndex`
- Если индекс вне bounds — возвращает nil

**`GetEventHistory(chain *ExpeditionChain) []ExpeditionEvent`**
- Возвращает копию chain.Events

### 2.2 Prompt template

**Файл:** `server/internal/game/planet_survey/expedition_event_prompt.txt` (NEW)

Prompt template для LLM. Embed'ится через `go:embed`.

В `expedition_chain.go`:
```go
import _ "embed"

//go:embed expedition_event_prompt.txt
var promptTemplate string
```

### 2.3 Добавить методы в Planet

**Файл:** `server/internal/game/planet.go`

```go
// CanStartExpedition checks if player can start a new expedition chain.
func (p *Planet) CanStartExpedition() bool {
    if !p.BaseOperational() {
        return false
    }
    if _, ok := p.Research.GetCompleted()["planet_exploration"]; !ok {
        return false
    }
    activeCount := 0
    for _, ch := range p.ExpeditionChains {
        if ch.Status == "active" {
            activeCount++
        }
    }
    return activeCount < MaxActiveChains
}

// StartExpeditionChain creates a new expedition chain.
func (p *Planet) StartExpeditionChain(inventory map[string]float64) (*planet_survey.ExpeditionChain, *planet_survey.ExpeditionEvent, error)

// ResolveExpeditionChoice processes a player choice and generates next event.
func (p *Planet) ResolveExpeditionChoice(chainID string, choiceIndex int) (*planet_survey.ExpeditionEvent, error)

// GetActiveExpeditionChain returns the active chain by ID.
func (p *Planet) GetActiveExpeditionChain(chainID string) *planet_survey.ExpeditionChain

// GetExpeditionChains returns all chains (active + recent completed).
func (p *Planet) GetExpeditionChains() []*planet_survey.ExpeditionChain

// ReturnExpeditionInventory returns inventory to planet and removes chain from memory.
func (p *Planet) ReturnExpeditionInventory(chainID string) error
```

### 2.4 Добавить методы сохранения в БД

**Файл:** `server/internal/game/planet_survey/expedition_chain.go`

```go
// SaveChainToDB saves expedition chain to expedition_chains table.
func SaveChainToDB(chain *ExpeditionChain, db *db.Database) error

// SaveEventsToDB saves all events in a chain to expedition_events table.
func SaveEventsToDB(chainID string, events []ExpeditionEvent, db *db.Database) error

// LoadActiveChains loads all active expedition chains from DB for a planet.
func LoadActiveChains(planetID string, db *db.Database) ([]*ExpeditionChain, error)

// LoadChainEvents loads all events for a chain from DB.
func LoadChainEvents(chainID string, db *db.Database) ([]ExpeditionEvent, error)
```

Реализация `SaveEventsToDB`:
- DELETE существующие события chain_id, затем INSERT все события
- Использует ON CONFLICT или DELETE + INSERT для idempotency

Реализация `LoadActiveChains`:
- SELECT из expedition_chains WHERE planet_id = $1 AND status = 'active'
- Для каждой цепочки загружает события через LoadChainEvents
- Восстанавливает Events slice в памяти

### 2.5 Обновить game.go — load

**Файл:** `server/internal/game/game.go`

В `loadPlanetFromDB` и `loadPlanetsFromDB` добавить после `loadLocations(p)`:

```go
// Load expedition chains
if err := loadExpeditionChains(planet); err != nil {
    log.Printf("Error loading expedition chains for planet %s: %v", planet.ID, err)
}
```

Новая функция:
```go
func loadExpeditionChains(p *Planet) error {
    if p.game == nil || p.game.db == nil {
        return nil
    }
    chains, err := planet_survey.LoadActiveChains(p.ID, p.game.db)
    if err != nil {
        return err
    }
    p.ExpeditionChains = chains
    return nil
}
```

### 2.6 Обновить planet_state.go — экспедиции в GetState

**Файл:** `server/internal/game/planet_state.go`

В `GetState()` добавить:
```go
"can_start_expedition": p.CanStartExpedition(),
```

### 2.7 Тесты

**Файл:** `server/internal/game/planet_survey/expedition_chain_test.go` (NEW)

- `TestValidateInventory` — valid, over limit, negative, unknown resources, empty
- `TestParseReward` — nested format → flat, unknown resources ignored
- `TestApplyRewardToInventory` — add, clamp to 0, clamp to 1000
- `TestGetInventorySize` — sum of all resources
- `TestParseEventResponse` — valid JSON, markdown-wrapped, invalid JSON, nested rewards
- `TestBuildPrompt` — contains planet name, inventory, events
- `TestCompleteChain` — status = "completed", location set
- `TestFailChain` — status = "failed"
- `TestReturnInventoryToPlanet` — resources added back
- `TestResolveChoice_Reward` — inventory updated, clamp applied
- `TestGetCurrentEvent` — returns correct event, nil on out of bounds
- `TestCanStartExpedition` — operational + research + active count limit

**Запуск:**
```bash
go test -timeout 10s ./internal/game/planet_survey/...
go build -o /dev/null ./cmd/server/
```

## Деплой

```bash
./deploy.sh
go test -timeout 10s ./internal/game/planet_survey/...
go build -o /dev/null ./cmd/server/
```

## Зависимости

- Фаза 1 (DB migration) должна быть применена
- `openai` package уже существует (`server/internal/openai/client.go`)

## Примечания

- Event ID генерируется на бэкенде (UUID), а не берётся от LLM — это упрощает парсинг и гарантирует уникальность.
- `MaxActiveChains = 1` — одна активная цепочка на планету. Можно изменить константу, не трогая research.
- LLM вызов блокирующий, timeout 300s. На фазе 3 (API) потребуется настройка HTTP timeout для маршрутов с LLM.
